# Assembly Debugger
This project is a NASM-based x86 assembly program that reads machine code from a file, installs a signal handler, and inspects instructions by decoding their opcode bits. It appears to support stepping through execution, checking register values, and reporting whether the current instruction matches known operations such as mov, add, and, inc, dec, jmp, and call.
